import authActions from './auth.action';
import galleryActions from './gallery.action';
import paginationActions from './pagination.action';

const allActions = {
  authActions,
  galleryActions,
  paginationActions,
};

export default allActions;
